-- ===================================
-- 数据库迁移脚本：为 stock_daily 表添加 name 字段
-- ===================================
-- 
-- 变更说明：
-- 1. 新增 name 字段，用于存储股票中文名称
-- 2. 提升查询性能，避免每次搜索时从外部数据源获取名称
-- 3. 保持数据一致性，name 字段随日线数据同步更新
--
-- 执行时间：2026-02-24
-- 影响范围：stock_daily 表结构、数据同步逻辑、股票搜索接口
-- 

USE stock_analysis;

-- ===================================
-- 1. 添加 name 字段（向上迁移）
-- ===================================

-- 为 Doris 表添加 name 字段
ALTER TABLE `stock_daily` 
ADD COLUMN `name` VARCHAR(50) COMMENT '股票中文名称' 
AFTER `code`;

-- 验证字段添加成功
DESC `stock_daily`;

-- ===================================
-- 2. 回滚脚本（向下迁移）
-- ===================================
-- 如果需要回滚，执行以下语句：
-- 
-- ALTER TABLE `stock_daily` DROP COLUMN `name`;
-- 

-- ===================================
-- 3. 数据迁移说明
-- ===================================
-- 
-- 新增字段后，需要执行以下步骤：
-- 
-- 1. 运行历史数据补充脚本，为现有记录填充 name 字段：
--    python scripts/backfill_stock_names.py
-- 
-- 2. 后续数据同步时，name 字段将自动填充
-- 
-- 3. 股票搜索接口将优先从 name 字段获取名称
-- 

-- ===================================
-- 4. 索引优化建议
-- ===================================
-- 
-- 如果搜索性能需要优化，可以考虑添加索引：
-- 
-- CREATE INDEX idx_stock_daily_name ON stock_daily(name);
-- 
-- 注意：Doris 的 UNIQUE KEY 模型已包含 code+date 的唯一索引
-- name 字段的索引根据实际查询性能决定是否添加
-- 
